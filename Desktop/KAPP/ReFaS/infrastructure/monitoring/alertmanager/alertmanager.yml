global:
  resolve_timeout: 5m

route:
  receiver: "default-webhook"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: "default-webhook"
    webhook_configs:
      - url: "http://webhook-listener:5000/"
        send_resolved: true
