import { PageTitle } from "../../components/ui/PageTitle";

export function AnimalsPage() {
  return (
    <section>
      <PageTitle title="Animals" subtitle="Monitor livestock, health checks, and production metrics." />
      <p>Animal tracking and husbandry workflows are handled in this feature module.</p>
    </section>
  );
}
