import { PageTitle } from "../../components/ui/PageTitle";

export function MarketPage() {
  return (
    <section>
      <PageTitle title="Market" subtitle="Monitor price signals and market movement per crop." />
      <p>Market insights and pricing scenario inputs are isolated in this feature.</p>
    </section>
  );
}